# {{cookiecutter.project_name}}
Project description

## Local run
1. `pip install -r app/requirements.txt`
1. `cd web/`
1. `export PYTHONPATH=.`
1. `python main.py`

## Run in container
`docker-compose up --build -d`
