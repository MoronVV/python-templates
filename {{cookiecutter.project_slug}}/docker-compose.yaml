version: "3"

services:
  app:
    build: ./app
    container_name: {{cookiecutter.project_slug}}
    restart: unless-stopped
    ports:
      - 8080:8080
    environment:
      - SERVICE={{cookiecutter.project_slug}}
      - IN_DOCKER=yes
    volumes:
      - ./log:/var/log
